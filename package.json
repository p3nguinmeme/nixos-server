{
  "name": "node-webserver",
  "version": "1.0.0",
  "description": "test application for docker process to be run on nixos",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  }
}
